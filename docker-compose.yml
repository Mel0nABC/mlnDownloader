services:
  mlndownloder:
    image: mel0nabc/mlndownloader:latest
    container_name: mlndownloader
    ports:
      - "3000:8080"
    volumes:
      - ./downloads:/downloads
    # user: "1000:1000" # specific UID:GID from your user
